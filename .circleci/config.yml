version: 2.1

defaults: &defaults
  working_directory: ~/repo
  docker:
    - image: circleci/node:8.9.1

jobs:
  test:
    <<: *defaults
    steps:
      - run: npm install
      - run:
          name: Run tests
          command: npm test
workflows:
  app-testing:
    jobs:
      - test
